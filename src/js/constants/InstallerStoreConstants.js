var keyMirror = require('react/lib/keyMirror');

module.exports = keyMirror({
    RECEIVE_STEPS: null,    // When steps received from mock data, load them
    SET_STEP: null          // Sets current step
});
